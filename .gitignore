# local agent workspace
.claude/

# local backups (we evolve, never delete — kept on disk, not in git)
app.backup.*/

# backend runtime & secrets
backend/.env
scale-v2/.env
app/.env
backend/dist/
backend/node_modules/
scale-v2/node_modules/
backend/src/server.ts.before-rls-fix
backend/src/server.ts.save
